# Agent memory

## Learned User Preferences

- Prefer chunk cards without animation; user found chunk animation "weird."
- Prefer chunk layout that wraps into multiple rows rather than horizontal scroll.
- For UI/mobile polish, user has used the frontend-design skill; prefer that when improving UX/UI.
- Prefer concise docs and code; user has run deslop to trim verbosity.

## Learned Workspace Facts

- Project is a static Parcel app (index.html entry) with Tailwind; JS and CSS live in src/.
- Deployed to GitHub Pages at https://drugoi.github.io/qazaqsha-soyle/ via GitHub Actions; build uses `npm run build:gh` with `--public-url /qazaqsha-soyle/`.
